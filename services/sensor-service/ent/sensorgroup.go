// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/skni-kod/iot-monitor-backend/services/sensor-service/ent/sensorgroup"
)

// SensorGroup is the model entity for the SensorGroup schema.
type SensorGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name of the sensor group
	Name string `json:"name,omitempty"`
	// Description of the sensor group
	Description string `json:"description,omitempty"`
	// Color for UI display (hex format)
	Color string `json:"color,omitempty"`
	// Icon identifier for UI
	Icon string `json:"icon,omitempty"`
	// ID of the user who owns the group
	UserID int64 `json:"user_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SensorGroupQuery when eager-loading is set.
	Edges        SensorGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SensorGroupEdges holds the relations/edges for other nodes in the graph.
type SensorGroupEdges struct {
	// Sensors in this group
	Sensors []*Sensor `json:"sensors,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SensorsOrErr returns the Sensors value or an error if the edge
// was not loaded in eager-loading.
func (e SensorGroupEdges) SensorsOrErr() ([]*Sensor, error) {
	if e.loadedTypes[0] {
		return e.Sensors, nil
	}
	return nil, &NotLoadedError{edge: "sensors"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SensorGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sensorgroup.FieldID, sensorgroup.FieldUserID:
			values[i] = new(sql.NullInt64)
		case sensorgroup.FieldName, sensorgroup.FieldDescription, sensorgroup.FieldColor, sensorgroup.FieldIcon:
			values[i] = new(sql.NullString)
		case sensorgroup.FieldCreatedAt, sensorgroup.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SensorGroup fields.
func (sg *SensorGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sensorgroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sg.ID = int(value.Int64)
		case sensorgroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sg.Name = value.String
			}
		case sensorgroup.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				sg.Description = value.String
			}
		case sensorgroup.FieldColor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field color", values[i])
			} else if value.Valid {
				sg.Color = value.String
			}
		case sensorgroup.FieldIcon:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field icon", values[i])
			} else if value.Valid {
				sg.Icon = value.String
			}
		case sensorgroup.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				sg.UserID = value.Int64
			}
		case sensorgroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sg.CreatedAt = value.Time
			}
		case sensorgroup.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sg.UpdatedAt = value.Time
			}
		default:
			sg.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SensorGroup.
// This includes values selected through modifiers, order, etc.
func (sg *SensorGroup) Value(name string) (ent.Value, error) {
	return sg.selectValues.Get(name)
}

// QuerySensors queries the "sensors" edge of the SensorGroup entity.
func (sg *SensorGroup) QuerySensors() *SensorQuery {
	return NewSensorGroupClient(sg.config).QuerySensors(sg)
}

// Update returns a builder for updating this SensorGroup.
// Note that you need to call SensorGroup.Unwrap() before calling this method if this SensorGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (sg *SensorGroup) Update() *SensorGroupUpdateOne {
	return NewSensorGroupClient(sg.config).UpdateOne(sg)
}

// Unwrap unwraps the SensorGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sg *SensorGroup) Unwrap() *SensorGroup {
	_tx, ok := sg.config.driver.(*txDriver)
	if !ok {
		panic("ent: SensorGroup is not a transactional entity")
	}
	sg.config.driver = _tx.drv
	return sg
}

// String implements the fmt.Stringer.
func (sg *SensorGroup) String() string {
	var builder strings.Builder
	builder.WriteString("SensorGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sg.ID))
	builder.WriteString("name=")
	builder.WriteString(sg.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(sg.Description)
	builder.WriteString(", ")
	builder.WriteString("color=")
	builder.WriteString(sg.Color)
	builder.WriteString(", ")
	builder.WriteString("icon=")
	builder.WriteString(sg.Icon)
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", sg.UserID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sg.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sg.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SensorGroups is a parsable slice of SensorGroup.
type SensorGroups []*SensorGroup
